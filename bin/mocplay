#!/usr/bin/env bash

if [[ ! $(pidof mocp) ]]; then
    mocp --server
    mocp --play
else
    if [[ $(mocp -i | awk '{print $2}') == STOP ]]; then
         mocp --play
    else
         mocp --toggle-pause
    fi
fi
